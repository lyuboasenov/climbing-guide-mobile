<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
   xmlns:resources="clr-namespace:Climbing.Guide.Forms.Resources.Strings"
   xmlns="http://xamarin.com/schemas/2014/forms"
   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
   xmlns:converters="clr-namespace:Climbing.Guide.Forms.Converters"
   xmlns:routes="clr-namespace:Climbing.Guide.Forms.Views.Routes"
   x:Class="Climbing.Guide.Forms.Views.Routes.RouteView"
   Title="{Binding Title}" >
   <ContentPage.Resources>
      <converters:PathToImageSourceConverter x:Key="PathToImageSourceConverter" />
   </ContentPage.Resources>

   <Grid>
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="*" />
         <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto" />
         <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <ScrollView Grid.Row="1" Grid.ColumnSpan="2">
         <Grid>
            <Grid.ColumnDefinitions>
               <ColumnDefinition Width="Auto" />
               <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
               <RowDefinition Height="Auto" />
               <RowDefinition Height="Auto" />
               <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Text="{Binding Source={x:Static resources:Routes.Info}}" />
            <StackLayout Grid.Row="0" Grid.Column="1">
               <Label Text="{Binding Route.Info}" />
            </StackLayout>

            <Label Grid.Row="1" Text="{Binding Source={x:Static resources:Routes.Map}}" />
            <StackLayout Grid.Row="1" Grid.Column="1">
               <Label Text="Map here!" />
            </StackLayout>

            <Label Grid.Row="2" Text="{Binding Source={x:Static resources:Routes.Schema}}" />
            <routes:SchemaView Grid.Row="2"
                               Grid.Column="1"
                               SchemaLocalPath="{Binding LocalSchemaThumbPath}"
                               SchemaRoute="{Binding SchemaRoute}" />
         </Grid>
      </ScrollView>

   </Grid>
</ContentPage>
